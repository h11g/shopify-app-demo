query Input {
  discountNode {
    metafield(namespace: "order-discount", key: "percentage") {
      value
    }
  }
  cart {
    lines {
      quantity
      merchandise {
        ...on ProductVariant {
          id
        }
      }
    }
  }
}
